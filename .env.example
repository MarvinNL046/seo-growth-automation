# Keep real values outside Git. The scripts never print these values.
DATAFORSEO_LOGIN=
DATAFORSEO_PASSWORD=

# Alternative to LOGIN + PASSWORD: base64(login:password).
# DATAFORSEO_BASE64=

# Path to an ignored Google OAuth authorized_user JSON file with read-only
# Search Console access. Never commit that file or its contents.
GSC_OAUTH_CREDENTIALS=
